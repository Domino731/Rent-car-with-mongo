import {Modal} from "./Components/Modal";

export const CarEquipmentAndImages = () => {
    return <Modal onSubmit={e => console.log(e)} validationSchema={{}} initialValues={{}}>
        Test
    </Modal>
}